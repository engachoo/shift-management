# 大日産業グループウェア プロジェクト

## ⚠️ 重要：以前の画像埋め込み指示はすべて取り下げ済み

過去のセッションで行った以下の作業は**完了・終了**しています。再開・再実行しないでください：

- 外部URL（block47.jp、welpia-hashima.com 等）から画像を curl でダウンロードする
- 画像を base64 変換して CSS に埋め込む
- `/tmp/welpia_hero.jpg` など一時ファイルへの画像保存
- `base64 -i ... -o ...` コマンドの実行

これらの指示は**取り下げ済み**です。APIエラー（"Could not process image"）の原因となるため実行禁止です。

## ログイン背景画像の現在の方針

ログイン画面の背景画像はすべてローカルファイル参照を使用します：

- メイン背景: `slide1.jpg`（同ディレクトリ）
- サブ背景: `slide2.jpg`（同ディレクトリ）

**CSS に base64 画像を埋め込むことは禁止**です。

## ファイル構成

| ファイル | 用途 |
|---|---|
| `groupware.html` | 大日産業グループウェア |
| `shift_management.html` | 大日産業シフト管理 |
| `shift_management_welpia.html` | ウェルピアシフト管理 |
| `slide1.jpg` | ログイン背景メイン画像 |
| `slide2.jpg` | ログイン背景サブ画像 |

## セッション再開時の注意

再開（resume）する際は、上記の取り下げ済み作業を引き継がず、ユーザーの新しい指示を待ってください。
